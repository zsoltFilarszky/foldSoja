package Tasks;

import sun.reflect.generics.reflectiveObjects.NotImplementedException;

import java.util.ArrayList;

/**
 * Created by hackerhanry on 6/23/16.
 */
public class ArrayValueDistanceTaskClass {
    //TASK [HUN]
    /*Adott egy integer input array. pl: A[0]=1 A[1]=-3 A[2]=3  képezz ezekből párokat, és számísd ki
    az érték-távolság különbséget mindegyik párra, és add vissza ezeknek a maximumát.
    pl: értékpárok egy 3 elemű tömb esetén: (0,0);(0,1);(0,2);(1,1);(1,2);(2,2)
     érték-távolság számítás: (0,0) -> A[0]+A[0]+(0-0)=value
     (i,j)->A[i]+A[j]+(j-i) =valami
     Írj kódot, ami ezeknek az összegeknek a maximumát visszaadja

        [ENG]
        Given an integer array A, return the maximum possible sum-distance between two elements.
         The sum-distance is defined as A[i] + A[j] + (i - j) for i > j
        For example with A = [8, 2, 4, 9, 5, 8, 0, 3, 8, 2] the max sum-distance is 24 achieved with i=0 and j=8
    * */

    public int solution(int[] A){
        throw new NotImplementedException();
    }

}
